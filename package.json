{
  "name": "birdwing",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "lerna run build",
    "clean": "rimraf packages/**/dist packages/**/*/dist dist"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/node": "20.11.30",
    "lerna": "^8.1.2",
    "typescript": "^4.6.0"
  }
}
